name: CoderCo Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4

    - name: hello world custom action
      uses: shuaybali-commits/coderco-custom@main
      with:
        who-to-greet: 'CoderCo Team'
