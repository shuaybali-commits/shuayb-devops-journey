# Bandit Level 03 → 04

## Objective
Find the password stored in a hidden file inside the `inhere` directory.

## Key concept
Hidden files start with `.` and require `ls -a` to be listed.

## Commands used
```bash
ls
cd inhere
ls -a
cat ./...
