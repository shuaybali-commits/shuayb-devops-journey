# Bandit Level 10 → 11

## Objective
Decode the Base64-encoded password stored in `data.txt`.

## Key concept
Base64 is an encoding format, not encryption. Use `base64 -d` to decode.

## Commands used
```bash
base64 -d data.txt
