# Bandit Level 04 → 05

## Objective
Find the password in the only human-readable file in `inhere/`.

## Key concept
Use `file` to identify file types, then read the ASCII text file.

## Commands used
```bash
cd inhere
file ./*
cat ./-file07
